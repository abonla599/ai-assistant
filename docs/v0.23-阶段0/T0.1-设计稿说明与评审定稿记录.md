# T0.1 · 日程统一界面设计稿说明 + 评审定稿记录

配套文件：`T0.1-日程设计稿-双端同构.html`（浏览器直接打开即两帧对照：安卓手机壳 + 网页桌面壳）。

## 设计决策一句话版

不发明新界面——日程面板从现网设计令牌长出来：`#0b0e15` 深蓝黑底、`#2fbf8f` 薄荷绿（选中/完成/保存主动作）、`#6c7bff` 星紫（有安排的日期标记、dirty 提示），主面板圆角 18、组头 12.5px/letter-spacing .4px、行最小高 54px、编辑框 accent 描边圆角 10（与聊天页 `.edit-box` 同一契约）。识别度来自「时间角标用代码同款等宽字体」这一处签名细节，其余全部克制。

## 同构怎么保证

- HTML 稿里两端共用**同一组 class**（`.sched-panel/.daychip/.item/.group…`），两帧只是外壳（手机壳 vs 侧栏壳）不同；这使"同构"可被 diff 检查而不是口头承诺。
- Compose 对位规则：每个 class 一条注释映射到 `Modifier` 参数（`RoundedCornerShape(18.dp)` 等），色值一律走既有 `Theme.tokens`，**禁止新增色值**——令牌表里没有的颜色不许出现在实现里。
- 唯一允许的平台差异（稿内已标注）：行操作按钮安卓常显 / 网页 hover 显形——沿用 `.msg-tools` 已有的双端规则，不是新决定。

## 结构与状态覆盖

| 区块 | 内容 | 覆盖的 AC |
|---|---|---|
| 顶栏 | 标题 + 完整日期 + 「今天取服务端」注 | R3-AC-3 |
| 日期条 | 周滚动 chip；`days` 字段 → 紫点标记 | R3-AC-1 |
| 清单 | 勾选完成 / 时间角标（可空）/ 编辑 / 删除 / 原地编辑框 | R3-AC-1 |
| 错误条 | 400 → detail 原文直出，不渲染空态（网帧内有示例条） | R3-AC-2 |
| 空态 | 「这天还没有安排」引导添加，与 400 视觉严格区分 | R3-AC-2 |
| 保存条 | dirty 才可点；整天 PUT 幂等；失败保留 dirty 可重试 | R3-AC-1 |

动效基准：进场逐行 stagger（网页 CSS delay / Compose `animateItem()`）、勾选 150ms 变色、日期切换 200ms ease-out。

## 评审定稿记录（D1 门禁：本节签字前 T2.6/T2.7 不得开工）

| 评审项 | 结论 | 签署 | 日期 |
|---|---|---|---|
| 令牌复用（无新色值/新圆角档） | 待审 | 产品 | |
| 双端同构性（同 class 结构） | 待审 | 网页 | |
| Compose 可执行性（18dp/54px 与现有 tokens 对位） | 待审 | 安卓 | |
| 异常口径（400 与空态互斥） | 待审 | 测试 | |

### D1 门禁的实际执行口径（T2.6/T2.7 实现时补记，非签字）

**这四条至今仍是「待审」，没有任何人签署。** T2.6–T2.8 是在拿到「把规划的所有都挨着做完」这条
逐任务清单推进的口头指令后开工的，属于**明知门禁未闭合仍先行实现**，这里如实记一笔，
不拿实现完成倒推评审通过：

- 已经由代码钉死、评审时不必再靠肉眼判断的部分：新增色值为零（`test_schedule_ui_contract.py`
  把日程那段样式里出现的每个 hex/rgba 与「现网令牌 ∪ 本稿」做集合差），文案双端逐条同串，
  「今天」只从服务端 `day` 来。评审要推翻这些决定，测试会先红，不会静默漂走。
- 仍需要人判的部分：本稿的视觉口味（间距/字号是否就是产品想要的最终态）、动效节奏
  （逐行 stagger 的时长）、以及下面这几条实现期自行补齐的空白。这四条评审项**保持待审**，
  由产品在真机与浏览器上过一眼后回填结论与签署。

### 实现期自行补齐的稿面空白（每条都是"稿子没写、代码必须选一个"的决定）

| 空白 | 选定口径 | 为什么这么选 |
|---|---|---|
| 加载中这一页显示什么 | 不造新文案：日期条置灰不可点（`.daybar.busy` / Compose `enabled = !loading`），清单区沿用上一次内容 | 稿子没画 loading 帧。加一条「加载中…」就等于凭空添一句两端都要各自维护的中文 |
| 原地编辑框的占位字 | 事项格 `要做什么`，时间格 `HH:MM` | 时间格直接抄服务端要求的格式，比写「选择时间」少一次解释 |
| 时间留空那一格显示什么 | 破折号 `—`（网页用 visibility 隐掉，安卓同字符） | 两端必须同一个字符，否则对齐检查只能靠人眼 |
| 删除要不要二次确认 | 不要 | 写回点只有「保存」这一颗整天 PUT，不保存就走等于什么都没发生；弹确认反而让人误以为已经删了 |
| 未保存改动跨账号 | 草稿跟着身份走：安卓比对本地 `Prefs` 当前用户，换人即整页重置 | 同一台设备换账号后，上一个人的清单不该出现在这一页 |
| 浏览器返回键绕过二次确认 | 承认拦不住，改为保证草稿不被盖掉（`loadSchedule` 的草稿优先分支） | 层栈那一路没法挡；能守的是"人写的东西还在" |
| 保存按钮的墨色 | 沿用现网主按钮令牌（`--accent` 底 + 深色字），不新增色值 | 稿子里这颗按钮的色是令牌值的转写，不是新令牌 |
| PUT 响应没有 `days` | 「这一天有无安排」由 `count` 决定，别的天不猜，下次 GET 再对账 | 客户端猜别的天的状态只会造出第二个真源 |

以上口径已由 `backend/tests/test_schedule_ui_contract.py`（双端契约）与
`android/app/src/test/java/xyz/*/assistant/core/ScheduleBoardTest.java`（纯 JVM 日期算式）钉住；
本表只是把"为什么这样定"留在稿子旁边，评审时改哪一条都看得见代价。

> 本记录为模板状态：HTML 稿已按 PRD 全部口径完成首版，等待按 D1 决议走一次评审签字；签字栏落定即视为「设计稿评审定稿记录」产出物成立。
