# T0.3 · v0.22 现状盘点复核 + UI 缺口清单

盘点人：前端开发（Waker）｜日期：2026-09-26｜基线：`main@557e4f65`
证据均为 worktree 实测行号。结论标记：【已实现】【部分】【缺失】。

## 一、任务要求的两处硬核对（PRD E7 / T0.3 原文）

| 核对项 | 结果 | 证据 |
|---|---|---|
| 原生 `Api.kt` 已有 messages PUT | ✅ 在 `:160-171`（PRD 写的 :169 即 PUT 调用行） | `call("/v1/sessions/" + enc(id) + "/messages", "PUT", …)` |
| 原生 `Api.kt` 已有 export-ticket | ✅ 在 `:174-175`（PRD 写的 :175） | `callJson("/v1/sessions/" + enc(id) + "/export-ticket", "POST")` |

## 二、R1 消息编辑 + 重新生成 ——【已实现】（PRD 缺口定性过时）

| 子项 | 状态 | 证据 |
|---|---|---|
| 消息操作条（复制/编辑/重新生成/👍/👎/删除） | 已实现 | `ui/ChatUi.kt:926-944`，按钮集合与显隐规则同网页 `index.html:370-376` + `app.js:1333-1340` |
| 原地编辑框（对位 `.edit-box`） | 已实现 | `ChatUi.kt:880-897`；网页规格 `style.css:261` |
| 提交 → PUT 截断重发 | 已实现 | `ChatUi.kt:506-515` `onSaveEdit` → `persist()` `:268-276` → `Api.replaceMessages` |
| SSE 流式重生成 | 已实现 | `ChatUi.kt:514` `streamInto()`；`Api.streamChat` `Api.kt:323-343` |
| 流式中编辑置灰 | 逻辑拦截有、视觉置灰无（与网页一致，不构成双端差距） | `ChatUi.kt:305,370` `if (busy) return`；网页 `app.js:1442` 同样只拦不灰 |
| 失败回退非流式 + transient ⚠️ 气泡 | 已实现 | `ChatUi.kt:336-354`，与 `app.js:1520-1541` 逐支对齐 |

**剩余动作**：无需开发；T2.1/T2.2 降级为「双端回归核对」（并入 T2.3 的 1:1 基准验证）。

## 三、R2 会话导出 ——【已实现，两处小差异】

| 子项 | 状态 | 证据 |
|---|---|---|
| 导出按钮 | 已实现（入口在设置页，网页在聊天页——位置差异记录在案） | `ui/SettingsUi.kt:401` |
| export-ticket 签发 → 打开真实链接落下载 | 已实现（ACTION_VIEW 交浏览器，服务端 Content-Disposition 落盘） | `SettingsUi.kt:405-406` + `MainActivity.kt:73-78` |
| 空态文案逐字一致 | ✅ 「当前没有可导出的对话」== `app.js:2676` | `SettingsUi.kt:403` |
| 空态**判据**差异 | 部分：原生只判 `lastSessionId.isBlank()`；网页判「存在非 transient 消息」→ 有会话但全空时原生会签票导出空 Markdown | `SettingsUi.kt:403` vs `app.js:2676` |
| 票据过期处理 | 部分：委托服务端 404「导出链接无效或已过期」（`main.py:660-704`），原生 UI 未特判重签 | 见左 |

**剩余动作（T2.4/T2.5 收缩为）**：① 空态判据对齐网页（改一行谓词）；② 按 PRD R2-AC-3 补「过期自动重签一次」；③ D2 决议的「落系统 Downloads + 已下载通知」若要替代浏览器 intent，才需要动下载层。

## 四、R3 日程 ——【三端全缺失，是真缺口，且不是"补课"】

- 后端已就绪：`GET/PUT /v1/schedule`（`main.py:625-650`，数据层 `core/schedule.py`）；契约 `items[{text,at,done}] + days[]`。
- 网页版：**无**日程界面（`static/` 全量 grep 零命中）。
- 原生端：**无**日程 UI、`Api.kt` 无 schedule 方法（公开方法 43 个，逐一核对）。
- 原生现有「提醒」是本地闹钟（`Reminders.kt:16-22` 模块注释：服务端没有"提醒"这件事），与服务端日程无关。

**定性修正**：PRD 把 R3 写成"原生追平网页"不成立——两端都没有。T2.6/T2.7 是**双端同步新建**，同构设计稿（T0.1）因此是唯一的界面权威，这正是 D1 决议「稿不定稿不编码」的原因。

## 五、对拆解清单的影响（回写建议）

1. T2.1/T2.2 → 收缩为回归核对，与 T2.3 合并执行（省约 1.5 人日）。
2. T2.4/T2.5 → 剩余点收窄为上表三个小动作。
3. T2.6/T2.7 维持全量开发，依赖 T0.1 定稿。
4. R1/R2 的 PRD「验收映射」不变——已实现部分按 AC 回归即可。
