# v0.23（双端补齐）— 草稿，未发版

> **状态**：本文是 T3.5 的 Release 文案草稿，随代码一起留档；**尚未打 tag、尚未建 Release**。
> 出口条件里「0.22 → beta → 0.23 真机实测」还没跑（需要实物设备），所以这一版不能对外发布。
> 发布前需要补的东西集中写在文末「发版前必须补」一节。

## 加了什么

- **日程（网页 + 安卓，同一套界面）**：设置里多一个「日程」，一天一份清单。
  点日期条上的某天看那天的安排，有安排的日子底下带一个小圆点；添加、改文字、
  填时间（24 小时 `HH:MM`，可留空）、勾选完成、删除都在原地做，**改动要点「保存」
  才写回服务器**——保存是整天替换，同一份内容存两次结果一样，重放不会出错。
  「今天」以服务器日期为准：设备把系统时间改了，问出来的也不是别人的一天。
- **安卓原生：编辑消息并重新生成**：长按自己的消息可改可重发，改完从那条之后重新生成，
  与网页版同一套行为。
- **安卓原生：导出本次对话**：一键导出，文件直接落到系统「下载」目录并给一条已下载通知
  （本版不做分享面板）。链接过期会自动重签一次再试。
- **应用内一键更新**：关于页能查新版、下载、校验、拉起安装；下载与校验只走自建服务端
  这一个入口。

## 修了什么

- 更新链路的地基问题：此前客户端直连 GitHub 取发布信息，GitHub 一侧漂了就表现为
  「检查更新失败」而自家服务端其实好着。现在这条直连被封死，改走自建服务端。
- 日程与导出的错误提示不再由客户端自己造句：服务端怎么说，界面就怎么显示
  （例如日期写错时提示「日期要写成 YYYY-MM-DD」），也不会把一句问错了的话
  渲染成「这天还没有安排」。

## 注意（这一跳请手动装一次）

- **v0.22 → v0.23 需要手动安装一次**：应用内更新是从 0.23 开始才有的能力，
  手上还是 0.22 的设备这一次仍要下载 APK 自己装。装上之后，
  **0.23 → 0.24 那一跳起才是在 App 里点一下就完成**。
- 网页版随服务端部署即生效，没有独立发布动作。
- 日程数据存在服务端，换设备、换浏览器登录同一账号看到的是同一份。

## 发版前必须补（当前为空的格子）

| 项 | 缺什么 | 谁 |
|---|---|---|
| T3.1 语音输入真机矩阵 | ≥2 厂商 ROM 的实测记录（模板：`docs/v0.23-T1.9-beta实测留档模板.md`、`docs/v0.23-阶段0/T0.2-真机测试矩阵模板.md`） | QA + 实物设备 |
| T3.2 权限异常机型兜底 | 状态条指引可执行、不崩溃不静默的机型证据 | QA + 实物设备 |
| T3.3 还原度逐屏点验 | 安卓侧每一屏对照网页（本机无 Android SDK，Compose 只过了编译） | 产品 + QA |
| T3.4 1:1 契约终审 | R1/R2/R3 三功能双端逐屏对照签字；`docs/v0.23-阶段0/T0.1-设计稿说明与评审定稿记录.md` 里 D1 四条仍为「待审」 | 产品 |
| T3.5 tag → CI 六流水线 | 上面四项闭合后打 tag；正式 0.23 发布 | CI + 产品 |

核对证据与已知差异清单见 `docs/v0.23-T3.3-T3.4-还原度与双端契约核对记录.md`。
