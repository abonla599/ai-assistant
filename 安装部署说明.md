# AI智能助手 - 安装部署说明

<div align="center">

**版本号：**V1.2  
**编写日期：**2026年5月28日  
**开发团队：**软件学院第十一组

</div>

---

## 一、概述

本文档详细介绍AI智能助手系统的安装和部署步骤，包括开发环境搭建、桌面应用安装、服务器部署等多种方式。请根据您的使用场景选择合适的部署方案。

---

## 二、运行环境要求

### 2.1 桌面应用（Windows）

#### 最低配置：
- 操作系统：Windows 10 64位（版本1903或更高）
- 处理器：Intel Core i5-8代或同等性能AMD处理器
- 内存：8GB RAM
- 硬盘：2GB可用空间
- 网络：宽带互联网连接（用于调用AI模型API）

#### 推荐配置：
- 操作系统：Windows 11 64位
- 处理器：Intel Core i7-12代或更高
- 内存：16GB RAM
- 硬盘：5GB可用空间（SSD更佳）
- 网络：高速宽带互联网连接

#### 可选组件：
- Docker Desktop 4.20或更高版本（如需使用代码执行功能）

### 2.2 服务器部署（Linux）

#### 最低配置：
- 操作系统：Ubuntu 20.04 LTS或CentOS 8
- 处理器：2核CPU
- 内存：4GB RAM
- 硬盘：10GB可用空间
- 网络：公网IP或可访问的域名

#### 推荐配置：
- 操作系统：Ubuntu 22.04 LTS
- 处理器：4核CPU或更高
- 内存：8GB RAM或更高
- 硬盘：50GB SSD
- 网络：带宽10Mbps以上

#### 必需组件：
- Docker 20.10或更高版本
- Docker Compose 2.0或更高版本
- Nginx（可选，用于反向代理）

---

## 三、桌面应用安装（推荐普通用户）

### 3.1 下载安装包

从项目发布页面下载最新版本的安装包：
- 文件名：AI智能助手_Setup.exe
- 文件大小：约150MB
- MD5校验码：[请在发布页面查看]

### 3.2 安装步骤

#### 步骤1：运行安装程序
双击"AI智能助手_Setup.exe"文件，如果系统提示"用户账户控制"，点击"是"允许运行。

#### 步骤2：选择安装语言
在安装向导首页选择"简体中文"，点击"确定"。

#### 步骤3：欢迎界面
阅读欢迎信息，点击"下一步"继续。

#### 步骤4：许可协议
阅读软件许可协议，勾选"我接受协议"，点击"下一步"。

#### 步骤5：选择安装位置
默认安装路径为：`C:\Program Files\AI智能助手`  
如需修改，点击"浏览"选择其他目录，然后点击"下一步"。

#### 步骤6：选择组件
勾选需要安装的组件：
- ☑ AI智能助手主程序（必选）
- ☑ Docker沙箱环境（如需代码执行功能，建议勾选）
- ☑ 桌面快捷方式
- ☑ 开机自动启动（可选）

点击"下一步"继续。

#### 步骤7：准备安装
确认安装设置无误，点击"安装"开始安装过程。

#### 步骤8：安装进度
等待安装完成，进度条显示当前安装状态。此过程可能需要3-5分钟。

#### 步骤9：安装完成
安装成功后，勾选以下选项（按需）：
- ☑ 运行AI智能助手
- ☑ 查看README文件

点击"完成"退出安装向导。

### 3.3 首次运行配置

#### 步骤1：启动应用
双击桌面快捷方式"AI智能助手"，或在开始菜单中找到并打开。

#### 步骤2：后端服务启动
首次运行时，系统会自动启动后端服务。如果防火墙弹出提示，请点击"允许访问"。

#### 步骤3：配置API密钥
在应用主界面点击右上角"设置"图标，进入设置页面：
- 找到"API配置"部分
- 输入您的API密钥（可从API易中转站获取）
- 选择默认模型（推荐：gpt-4o）
- 点击"保存"

#### 步骤4：测试连接
在设置页面点击"测试连接"按钮，确认API配置正确。如果显示"连接成功"，即可开始使用。

#### 步骤5：开始对话
返回主界面，在输入框中输入您的问题，按Enter键或点击发送按钮开始对话。

### 3.4 常见问题

**Q1：安装时提示"Docker未安装"？**  
A：如果您需要使用代码执行功能，请先安装Docker Desktop。下载地址：https://www.docker.com/products/docker-desktop/  
如果暂时不需要代码执行功能，可以取消勾选"Docker沙箱环境"组件继续安装。

**Q2：启动时后端服务无法启动？**  
A：检查以下几点：
- 确认端口8000未被其他程序占用
- 检查防火墙是否阻止了应用访问网络
- 尝试以管理员身份运行应用

**Q3：API密钥配置后仍然提示错误？**  
A：请确认：
- API密钥是否正确复制（注意前后空格）
- 账户余额是否充足
- 选择的模型是否在您的API权限范围内

**Q4：如何卸载应用？**  
A：通过Windows"设置"→"应用"→"应用和功能"，找到"AI智能助手"，点击"卸载"。或者使用控制面板的程序和功能进行卸载。

---

## 四、开发环境搭建（推荐开发者）

### 4.1 前置条件

确保您的系统已安装以下软件：
- Python 3.10或更高版本
- Git
- Flutter SDK 3.16或更高版本
- Docker Desktop（可选，用于代码执行功能）
- VS Code或其他代码编辑器

### 4.2 克隆项目代码

打开命令行工具（CMD或PowerShell），执行以下命令：

```bash
# 克隆项目仓库
git clone https://github.com/your-repo/ai-assistant.git

# 进入项目目录
cd ai-assistant
```

### 4.3 配置Python虚拟环境

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境（Windows）
venv\Scripts\activate

# 激活虚拟环境（Linux/Mac）
source venv/bin/activate
```

### 4.4 安装Python依赖

```bash
# 升级pip
python -m pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt
```

**requirements.txt包含的主要依赖：**
- fastapi==0.104.0
- uvicorn==0.24.0
- chromadb==0.4.18
- openai==1.3.0
- sentence-transformers==2.2.2
- duckduckgo-search==3.8.0
- docker==6.1.3
- pytest==7.4.0
- httpx==0.25.0

### 4.5 配置环境变量

在项目根目录创建.env文件，参考.env.example填写配置：

```bash
# .env文件示例

# API配置
OPENAI_API_KEY=your-api-key-here
DEFAULT_MODEL=gpt-4o

# 数据库配置
CHROMA_HOST=localhost
CHROMA_PORT=8001

# 沙箱配置
SANDBOX_ENABLED=true
SANDBOX_TIMEOUT=10
SANDBOX_MEMORY_LIMIT=256m

# 日志配置
LOG_LEVEL=INFO
```

### 4.6 启动Chroma数据库

ChromaDB可以以内嵌模式运行，无需单独启动服务。如果需要独立部署：

```bash
# 使用Docker启动Chroma
docker run -d --name chroma -p 8001:8000 ghcr.io/chroma-core/chroma:latest
```

### 4.7 启动后端服务

```bash
# 进入后端目录
cd backend

# 启动FastAPI服务
python app/main.py

# 或使用uvicorn直接启动
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

启动成功后，浏览器访问 http://localhost:8000/docs 可查看API文档。

### 4.8 启动前端应用

打开新的命令行窗口：

```bash
# 进入前端目录
cd mobile

# 获取Flutter依赖
flutter pub get

# 运行Windows桌面应用
flutter run -d windows

# 或构建发布版本
flutter build windows
```

### 4.9 验证安装

**测试后端API：**
```bash
# 健康检查
curl http://localhost:8000/api/health

# 预期返回
{"status": "ok", "version": "1.2"}
```

**测试对话接口：**
```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好", "stream": false}'
```

### 4.10 运行测试

```bash
# 运行所有测试
pytest tests/ -v

# 运行特定模块测试
pytest tests/test_tools.py -v

# 生成覆盖率报告
pytest tests/ --cov=backend/app --cov-report=html
```

---

## 五、Docker部署（服务器版）

### 5.1 前置条件

确保服务器已安装：
- Docker 20.10或更高版本
- Docker Compose 2.0或更高版本

**安装Docker（Ubuntu）：**
```bash
# 更新包索引
sudo apt update

# 安装依赖
sudo apt install apt-transport-https ca-certificates curl software-properties-common

# 添加Docker官方GPG密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker.gpg

# 添加Docker仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装Docker
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 验证安装
docker --version
docker compose version
```

### 5.2 克隆项目代码

```bash
git clone https://github.com/your-repo/ai-assistant.git
cd ai-assistant
```

### 5.3 配置环境变量

复制.env.example为.env并修改配置：

```bash
cp .env.example .env
nano .env
```

修改以下关键配置：
```bash
# API密钥（必填）
OPENAI_API_KEY=your-api-key-here

# 服务端口
BACKEND_PORT=8000
CHROMA_PORT=8001

# 生产环境配置
ENVIRONMENT=production
DEBUG=false
```

### 5.4 启动服务

```bash
# 构建并启动所有服务
docker compose up -d --build

# 查看服务状态
docker compose ps

# 查看日志
docker compose logs -f
```

**docker-compose.yml包含的服务：**
- backend：FastAPI后端服务
- chroma：ChromaDB向量数据库
- sandbox：代码执行沙箱（可选）
- nginx：反向代理（可选）

### 5.5 配置Nginx反向代理（可选）

如果需要对外提供服务，建议配置Nginx：

```nginx
# /etc/nginx/sites-available/ai-assistant

server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # SSE支持
        proxy_http_version 1.1;
        proxy_buffering off;
        proxy_cache off;
    }
}
```

**启用配置：**
```bash
sudo ln -s /etc/nginx/sites-available/ai-assistant /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 5.6 配置HTTPS（推荐）

使用Let's Encrypt免费证书：

```bash
# 安装Certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo crontab -e
# 添加：0 0 1 * * certbot renew --quiet
```

### 5.7 数据持久化

Docker Compose已配置数据卷，确保数据持久化：

```yaml
volumes:
  chroma_data:
    driver: local
  feedback_data:
    driver: local
```

**数据存储在：**
- ChromaDB数据：./data/chroma_db
- 用户反馈：./data/feedback_data.json
- 日志文件：./logs/

### 5.8 备份与恢复

**备份数据：**
```bash
# 停止服务
docker compose down

# 备份数据目录
tar -czf backup_$(date +%Y%m%d).tar.gz data/

# 重启服务
docker compose up -d
```

**恢复数据：**
```bash
# 停止服务
docker compose down

# 恢复数据
tar -xzf backup_20260528.tar.gz

# 重启服务
docker compose up -d
```

### 5.9 监控与维护

**查看服务状态：**
```bash
docker compose ps
docker stats
```

**查看日志：**
```bash
# 查看所有服务日志
docker compose logs -f

# 查看特定服务日志
docker compose logs -f backend
```

**更新服务：**
```bash
# 拉取最新代码
git pull

# 重新构建并启动
docker compose up -d --build

# 清理无用镜像
docker image prune -f
```

---

## 六、参数配置详解

### 6.1 后端配置（.env文件）

```bash
# ==================== API配置 ====================
# OpenAI API密钥（必填）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxx

# 默认使用的模型
DEFAULT_MODEL=gpt-4o

# API基础URL（使用中转站时修改）
OPENAI_BASE_URL=https://api.openai.com/v1

# ==================== 数据库配置 ====================
# ChromaDB主机地址
CHROMA_HOST=localhost

# ChromaDB端口
CHROMA_PORT=8001

# 是否使用持久化存储
CHROMA_PERSIST=True

# 数据存储路径
CHROMA_DATA_DIR=./chroma_db

# ==================== 沙箱配置 ====================
# 是否启用沙箱
SANDBOX_ENABLED=true

# 代码执行超时时间（秒）
SANDBOX_TIMEOUT=10

# 内存限制
SANDBOX_MEMORY_LIMIT=256m

# CPU限制（百分比）
SANDBOX_CPU_LIMIT=50

# 支持的编程语言
SANDBOX_LANGUAGES=python,javascript

# ==================== 记忆配置 ====================
# 记忆检索返回的最大数量
MEMORY_TOP_K=5

# 记忆相似度阈值（0-1）
MEMORY_THRESHOLD=0.7

# 记忆衰减系数（每天）
MEMORY_DECAY_RATE=0.99

# ==================== 日志配置 ====================
# 日志级别：DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 日志文件路径
LOG_FILE=./logs/app.log

# 日志轮转大小（MB）
LOG_MAX_BYTES=10

# 保留的日志文件数量
LOG_BACKUP_COUNT=5

# ==================== 服务配置 ====================
# 后端服务监听地址
HOST=0.0.0.0

# 后端服务端口
PORT=8000

# 工作进程数
WORKERS=4

# 是否开启调试模式
DEBUG=false

# ==================== 安全配置 ====================
# JWT密钥（多用户模式下使用）
JWT_SECRET_KEY=your-secret-key-change-in-production

# Token过期时间（小时）
TOKEN_EXPIRE_HOURS=24

# CORS允许的源
CORS_ORIGINS=http://localhost:3000,http://localhost:8080
```

### 6.2 前端配置（mobile/lib/config.dart）

```dart
class AppConfig {
  // 后端API地址
  static const String apiBaseUrl = 'http://localhost:8000';
  
  // 流式响应超时时间（秒）
  static const int streamTimeout = 60;
  
  // 每页加载的历史消息数
  static const int historyPageSize = 20;
  
  // 主题配置
  static const bool defaultDarkMode = false;
  static const double defaultFontSize = 14.0;
}
```

### 6.3 Docker配置（docker-compose.yml）

```yaml
version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - CHROMA_HOST=chroma
      - CHROMA_PORT=8000
    volumes:
      - ./data/chroma_db:/app/chroma_db
      - ./data/feedback_data.json:/app/feedback_data.json
      - ./logs:/app/logs
    depends_on:
      - chroma
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G

  chroma:
    image: ghcr.io/chroma-core/chroma:latest
    ports:
      - "${CHROMA_PORT:-8001}:8000"
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

  sandbox:
    image: python:3.10-slim
    command: sleep infinity
    volumes:
      - ./sandbox:/workspace
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - sandbox_network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M

volumes:
  chroma_data:

networks:
  sandbox_network:
    driver: bridge
    internal: true
```

---

## 七、故障排查

### 7.1 后端服务无法启动

**症状：**运行python app/main.py后报错

**可能原因及解决方案：**

1. **端口被占用**
```bash
# Windows查看端口占用
netstat -ano | findstr :8000

# Linux查看端口占用
lsof -i :8000

# 杀死占用进程
taskkill /PID <PID> /F  # Windows
kill -9 <PID>            # Linux
```

2. **依赖缺失**
```bash
pip install -r requirements.txt
```

3. **环境变量配置错误**
检查.env文件格式是否正确，确保没有多余空格。

### 7.2 Docker容器启动失败

**症状：**docker compose up后容器退出

**可能原因及解决方案：**

1. **端口冲突**
修改docker-compose.yml中的端口映射：
```yaml
ports:
  - "8001:8000"  # 改为其他可用端口
```

2. **磁盘空间不足**
```bash
# 清理Docker缓存
docker system prune -a

# 检查磁盘空间
df -h
```

3. **权限问题**
```bash
# 将用户加入docker组
sudo usermod -aG docker $USER
# 重新登录生效
```

### 7.3 向量检索速度慢

**症状：**记忆检索耗时超过3秒

**可能原因及解决方案：**

1. **数据量过大**
```bash
# 清理过期记忆
# 在应用中执行记忆清理操作

# 优化Chroma配置
# 增加HNSW索引参数
```

2. **硬件性能不足**
考虑升级CPU或增加内存。

### 7.4 API调用失败

**症状：**对话时提示"API请求失败"

**可能原因及解决方案：**

1. **API密钥无效**
检查.env中的OPENAI_API_KEY是否正确。

2. **网络问题**
```bash
# 测试网络连接
curl https://api.openai.com/v1/models

# 检查代理配置
export HTTP_PROXY=http://proxy:port
export HTTPS_PROXY=http://proxy:port
```

3. **余额不足**
登录API提供商网站检查账户余额。

---

## 八、性能优化建议

### 8.1 后端优化

1. **启用缓存**
```python
from functools import lru_cache

@lru_cache(maxsize=1000)
def get_embedding(text: str):
    # 缓存嵌入向量计算结果
    return model.encode(text)
```

2. **异步处理**
使用FastAPI的异步特性，提高并发处理能力。

3. **数据库索引**
为ChromaDB添加合适的元数据过滤索引。

### 8.2 前端优化

1. **虚拟滚动**
对于长列表使用flutter_virtual_scroll包。

2. **图片懒加载**
仅在可见时加载图片资源。

3. **状态管理**
使用Provider或Riverpod优化状态更新。

### 8.3 Docker优化

1. **多阶段构建**
减小镜像体积：
```dockerfile
FROM python:3.10 as builder
RUN pip install --user -r requirements.txt

FROM python:3.10-slim
COPY --from=builder /root/.local /root/.local
```

2. **资源限制**
合理设置CPU和内存限制，避免资源争用。

3. **健康检查**
```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
  interval: 30s
  timeout: 10s
  retries: 3
```

---

## 九、安全建议

### 9.1 API密钥保护

- 不要将.env文件提交到Git仓库
- 在生产环境使用密钥管理服务（如AWS Secrets Manager）
- 定期轮换API密钥

### 9.2 网络安全

- 使用HTTPS加密传输
- 配置防火墙仅开放必要端口
- 使用VPN或SSH隧道访问管理接口

### 9.3 沙箱安全

- 始终保持Docker沙箱启用
- 定期更新Docker镜像
- 监控沙箱资源使用情况

### 9.4 数据安全

- 定期备份重要数据
- 对用户输入进行验证和过滤
- 记录安全相关的操作日志

---

## 十、技术支持

### 10.1 文档资源

- 项目README：项目根目录/README.md
- API文档：http://localhost:8000/docs（启动后端后访问）
- Flutter文档：https://docs.flutter.dev/
- FastAPI文档：https://fastapi.tiangolo.com/
- Docker文档：https://docs.docker.com/

### 10.2 问题反馈

如遇到问题，请通过以下方式反馈：
- GitHub Issues：https://github.com/your-repo/ai-assistant/issues
- 邮箱：support@ai-assistant.com
- 社区论坛：https://community.ai-assistant.com

**提交问题时请提供：**
- 问题描述
- 复现步骤
- 错误日志
- 系统环境信息

### 10.3 版本更新

关注项目Release页面获取最新版本：
https://github.com/your-repo/ai-assistant/releases

订阅更新通知，及时获取安全补丁和新功能。

---

## 附录A：快速启动命令汇总

### 开发环境：
```bash
# 后端
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
python app/main.py

# 前端（新窗口）
cd mobile
flutter pub get
flutter run -d windows
```

### Docker部署：
```bash
docker compose up -d --build
docker compose logs -f
```

### 测试：
```bash
pytest tests/ -v
```

---

## 附录B：目录结构说明

```
ai-assistant/
├── backend/              # 后端代码
│   ├── app/             # 应用主目录
│   │   ├── main.py      # 入口文件
│   │   ├── models/      # 数据模型
│   │   ├── routes/      # API路由
│   │   ├── services/    # 业务逻辑
│   │   └── utils/       # 工具函数
│   ├── tests/           # 测试代码
│   ├── requirements.txt # Python依赖
│   └── Dockerfile       # Docker镜像配置
├── mobile/              # Flutter前端
│   ├── lib/            # Dart源代码
│   ├── assets/         # 静态资源
│   └── pubspec.yaml    # Flutter依赖
├── data/               # 数据目录
│   ├── chroma_db/      # 向量数据库
│   └── feedback_data.json # 用户反馈
├── logs/               # 日志目录
├── docker-compose.yml  # Docker编排文件
├── .env.example        # 环境变量模板
└── README.md           # 项目说明
```

---

## 附录C：版本更新日志

### V1.2（2026-05-28）
- 完善向量记忆系统，增加衰减和摘要功能
- 实现自我成长机制，支持反馈驱动的优化
- 完成自动化测试套件，覆盖率达85%
- 优化Docker部署流程，支持一键启动
- 修复已知bug 15个

### V1.1（2026-04-10）
- 添加工具调用模块
- 实现计算器、搜索、代码执行工具
- 集成Docker沙箱环境
- 优化流式响应性能

### V1.0（2026-03-15）
- 完成项目基础框架
- 实现多模型接入功能
- 开发Flutter桌面应用原型
- 支持基本对话功能
